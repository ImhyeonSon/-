using UnityEngine;

public class MonsterStatus : MonoBehaviour
{
    // Monster의 Status를 저장해 두는 scripts 생성된 몬스터의 정보를 여기에 저장해둔다.
    // slow, stun등의 상태를 표현하기 위해 모든 정보를 가지고 있을 예정?

    private void Awake()
    {
        
    }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
